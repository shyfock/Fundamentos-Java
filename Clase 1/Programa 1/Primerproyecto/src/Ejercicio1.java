public class Ejercicio1 {
    public static void main(String[] args) {
        // Comentarios de línea
        /* -> abre Comentarios de bloque
        no importa la cantidad de líneas
        cierra comentarios -> 
        */
        // Las variables se deben declarar (asignar un tipo)
        int x; //Todas las sentencias terminan en (;)
        int y;
        String cad;
        char m;
        double a = 3.5;
        float d = 3.8f; // Para float debe escribirse f al final, sino lo toma como double
        x = 20;
        y = x + 1;
        cad = "Hola";
        m = 'm';
        System.out.println(a + d + cad + m + y);
        //var lista = Lista.of(1, 2, 3);


    }
}
